---
 - hosts: 127.0.0.1
   connection: local
   tasks:
      - include_vars: myvars.yml
      - name: What is My Name
        debug:
          msg: "Hellow, Your Full name is {{ name }} {{ lastname }}"
